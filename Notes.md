```
vagrant --version
vagrant --help
vagrant versin
vagrant box list
vagrant box add <base box>
vagrant box remove <base box>
vagrant init
vagrant up
vagrant ssh
vagrant halt
vagrant reload
vagrant suspend
vagrant resume
vagrant destroy <name>
vagrant global-status
vagrant box outdated
vagrant box update
```

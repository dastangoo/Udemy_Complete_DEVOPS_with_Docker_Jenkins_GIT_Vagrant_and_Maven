```
mkdir project_devops;cd $_
git init
git init project_devops2
git status
git status -s
git config --global --list | grep alias
git add test.rb
git commit -m "Message"
git rm --cached test.rb
git reset --soft HEAD^1
git reset HEAD test*
git log
```
